//par nome/valor
const saudacao = `opa`

function exec() {
    const saudacao = `eai chupão`
    return saudacao
}

// Objeto são grupos aninhados de pares chave/valor
const client = {
    nome: `Pedro`,
    idade: 33,
    peso: 90,
    endereco: {
        logradouro: `Rua muito Feliz`,
        numero: 123
    }
}

console.log(saudacao)
console.log(exec())
console.log(client)

console.log(console.log(3))